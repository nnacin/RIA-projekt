<% include ./header %>
<!--Start Content-->
<div id="content" class="col-xs-12 col-sm-10">
	<div class="row">
		<div class="col-md-3">
			<% employees.forEach(employee => { %>
				<% if (employee.username != "admin") { %>
				 		<div class="item-title">
							<a href="/staff/employee?id=<%= employee._id %>"><%= employee.username %></a>
						</div>
				<% } %>
		  <% }) %>
	  </div>
	  <div class="col-md-9">
  		<form method="POST" action="/staff/employee">
					<div class="form-group">
						<label>First Name</label>
						<input class="form-control" type="text" name="firstName" required>
					</div>
					<div class="form-group">
						<label>Last Name</label>
						<input class="form-control" type="text" name="lastName" required>
					</div>
					<div class="form-group">
						<label>Userame</label>
						<input class="form-control" type="text" name="username" placeholder="Username must be at least 6 characters long and unique." required>
					</div>
					<div class="form-group">
						<label>Email</label>
						<input class="form-control" type="email" name="email" required>
					</div>
					<div class="form-group">
						<label>Location</label>
						<select class="form-control" name="location" required>
							<% locations.forEach(function (location) { %>
								<option value="<%= location._id %>" ><%= location.name %></option>
							<% }) %>
						</select>
					</div>
					<button class="btn-primary">Save</button>
				</form>
	  </div>
  </div>
</div>
<!--End Content-->
<% include ./footer %>